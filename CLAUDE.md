# Project notes for Claude

## Workflow
- Merge PRs manually after creation. Do not enable auto-merge — the existing `auto-merge` GitHub Action is broken and will fail on every PR; ignore that failed check.
